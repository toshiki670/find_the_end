# frozen_string_literal: true

RSpec.describe FindTheEnd do
  it 'has a version number' do
    expect(FindTheEnd::VERSION).not_to be nil
  end

  it 'does something useful' do
    test1 = true
    expect(test1).to eq(true)
  end
end
